# Direct runtime dependencies verified with the production Python 3.13 line.
requests==2.32.5
urllib3==2.5.0
pandas==2.3.1
tzdata==2025.2
python-dotenv==1.2.2
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
dagster==1.13.14
dagster-webserver==1.13.14

# SQL Server ODBC driver for Accounts/Evolution extraction (requires the
# "ODBC Driver 17 for SQL Server" system driver to be installed separately).
pyodbc==5.2.0

# Test runner (provider tests mock HTTP and never call live APIs).
pytest==9.1.1
